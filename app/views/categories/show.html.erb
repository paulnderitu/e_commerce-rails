<h1><%= @category.name %></h1>
<h3><%= @category.brand_id %></h3>

<p><%= link_to "Edit", edit_category_path(@category) %></p>
 <p><%= link_to "Delete", category_path(@category),
                          :data => {:confirm => "You sure?",
                                    :method => "delete"} %></p>

  <p><%= link_to "Return to categories", categories_path %></p>
