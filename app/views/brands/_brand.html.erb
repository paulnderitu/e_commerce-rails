<%= form_for brand, remote: true do |f| %>
<%= f.check_box :old_brands%>
<%= f.submit "Update" %>
<%= f.label :old_brands, brand.title %>
<%= link_to "(remove)", brand, method: :delete, data: {confirm: "Are you sure?"}, remote: true %>
<% end %>
